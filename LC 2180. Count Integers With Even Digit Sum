// Concept of converting digits to chars and then adding them in case the n is odd then ans=n-1/2 else n/2

class Solution {
    public int countEven(int n) {
	
    return (String.valueOf(n).chars().map(Character::getNumericValue).sum()%2==0)?n/2: (n-1)/2;
}

}
